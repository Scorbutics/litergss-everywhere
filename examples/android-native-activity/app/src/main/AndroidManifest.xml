<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.RGSSNativeActivity"
        android:extractNativeLibs="true">

        <!-- Launcher Activity: Kotlin/KMP-based asset extraction and setup.
             Extracts Ruby runtime, sets environment variables, then launches NativeActivity. -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NativeActivity: SFML-based rendering driven by Ruby/LiteRGSS.
             android.app.lib_name points to librgss_runtime.so which contains:
             - SFML's ANativeActivity_onCreate (from sfml-main.a): sets up EGL, spawns thread -> main()
             - main() (from native_main.c): reads env vars, creates Ruby VM, executes script
             - Full LiteRGSS/LiteCGSS/SFML rendering stack -->
        <activity
            android:name="android.app.NativeActivity"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden">
            <meta-data
                android:name="android.app.lib_name"
                android:value="rgss_runtime" />
        </activity>
    </application>

</manifest>
