#!/bin/sh
#
# Configure script for LiteRGSS-Everywhere Build System
# Generates a Makefile for building LiteRGSS with optional Docker support
#

set -e

# Get the root directory
ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Ensure cmake/core symlink exists (points to ruby-for-android submodule)
if [ ! -d "$ROOT_DIR/cmake/core" ]; then
	echo "Creating symlink: $ROOT_DIR/cmake/core -> external/ruby-for-android/cmake/core"
	ln -s ../external/ruby-for-android/cmake/core "$ROOT_DIR/cmake/core"
fi

# Verify submodule is initialized
if [ ! -f "$ROOT_DIR/external/ruby-for-android/CMakeLists.txt" ]; then
	echo "Error: ruby-for-android submodule not initialized"
	echo "Run: git submodule update --init --recursive"
	exit 1
fi

# Default values
USE_DOCKER=1
TOOLCHAIN_PARAMS_LIST=""
PREFIX="/usr/local"
BUILD_DIR="build"
TARGET_DIR="target"
EXTRA_CMAKE_ARGS=""

# Parse command-line arguments
show_help() {
	cat << EOF
Usage: ./configure [OPTIONS]

Configuration Options:
  --without-docker          Build without Docker (requires local toolchain setup)
  --with-docker             Build with Docker (default)
  --with-toolchain-params=FILE
                            Specify toolchain parameters file (repeatable for multi-ABI)
                            (default: toolchain-params/arm64-v8a-android-toolchain.params)
  --enable-shared           Build shared libraries (.so files)
  --enable-static           Build static libraries (.a files) - default
  --enable-debug            Build with debug symbols and logging (CMAKE_BUILD_TYPE=Debug)
  --enable-release          Build with optimizations (CMAKE_BUILD_TYPE=Release)
  --prefix=PREFIX           Installation prefix (default: /usr/local)
  --build-dir=DIR           Build directory (default: build)
  --target-dir=DIR          Output directory for artifacts (default: target)
  --help                    Display this help message

Examples:
  ./configure
  ./configure --enable-shared
  ./configure --enable-debug
  ./configure --without-docker --with-toolchain-params=toolchain-params/x86_64-linux-toolchain.params --enable-static
  ./configure --with-toolchain-params=toolchain-params/x86_64-android-toolchain.params --enable-shared --enable-debug

  Multi-ABI Android build (produces a single AAR with both ABIs):
  ./configure --with-toolchain-params=toolchain-params/arm64-v8a-android-toolchain.params \\
              --with-toolchain-params=toolchain-params/x86_64-android-toolchain.params

  Multi-toolchain iOS build (device + simulator):
  ./configure --without-docker --enable-static --build-dir=build-ios --target-dir=target-ios \\
              --with-toolchain-params=toolchain-params/arm64-ios-device-toolchain.params \\
              --with-toolchain-params=toolchain-params/arm64-ios-simulator-toolchain.params

  CI/CD cross-platform builds (separate target dirs to avoid collisions):
  ./configure --target-dir=target-android --build-dir=build-android \\
              --with-toolchain-params=toolchain-params/arm64-v8a-android-toolchain.params \\
              --with-toolchain-params=toolchain-params/x86_64-android-toolchain.params
  make && make install
  ./configure --target-dir=target-linux --build-dir=build-linux --without-docker \\
              --with-toolchain-params=toolchain-params/x86_64-linux-toolchain.params
  make && make install

Environment Variables:
  ANDROID_NDK              Path to Android NDK (for Android builds)

EOF
	exit 0
}

# Parse options
while [ $# -gt 0 ]; do
	case "$1" in
		--without-docker)
			USE_DOCKER=0
			shift
			;;
		--with-docker)
			USE_DOCKER=1
			shift
			;;
		--with-toolchain-params=*)
			TOOLCHAIN_PARAMS_LIST="${TOOLCHAIN_PARAMS_LIST:+$TOOLCHAIN_PARAMS_LIST }${1#*=}"
			shift
			;;
		--enable-shared)
			EXTRA_CMAKE_ARGS="$EXTRA_CMAKE_ARGS -DBUILD_SHARED_LIBS=ON"
			shift
			;;
		--enable-static)
			EXTRA_CMAKE_ARGS="$EXTRA_CMAKE_ARGS -DBUILD_SHARED_LIBS=OFF"
			shift
			;;
		--enable-debug)
			EXTRA_CMAKE_ARGS="$EXTRA_CMAKE_ARGS -DCMAKE_BUILD_TYPE=Debug"
			shift
			;;
		--enable-release)
			EXTRA_CMAKE_ARGS="$EXTRA_CMAKE_ARGS -DCMAKE_BUILD_TYPE=Release"
			shift
			;;
		--prefix=*)
			PREFIX="${1#*=}"
			shift
			;;
		--build-dir=*)
			BUILD_DIR="${1#*=}"
			shift
			;;
		--target-dir=*)
			TARGET_DIR="${1#*=}"
			shift
			;;
		--help|-h)
			show_help
			;;
		*)
			echo "Error: Unknown option '$1'"
			echo "Run './configure --help' for usage information"
			exit 1
			;;
	esac
done

# Apply default if no toolchain params specified
if [ -z "$TOOLCHAIN_PARAMS_LIST" ]; then
	TOOLCHAIN_PARAMS_LIST="toolchain-params/arm64-v8a-android-toolchain.params"
fi

# Validate toolchain files and extract ABI names
TOOLCHAIN_COUNT=0
TOOLCHAIN_ABI_LIST=""
for tp in $TOOLCHAIN_PARAMS_LIST; do
	if [ ! -f "$ROOT_DIR/$tp" ]; then
		echo "Error: Toolchain parameters file not found: $tp"
		echo "Available toolchain files:"
		ls -1 "$ROOT_DIR"/toolchain-params/*.params 2>/dev/null || echo "  (none found)"
		exit 1
	fi
	TOOLCHAIN_COUNT=$((TOOLCHAIN_COUNT + 1))
	# Derive ABI name: prefer DANDROID_ABI from file content, otherwise use filename
	# e.g. "arm64-ios-device-toolchain.params" → "arm64-ios-device"
	ABI=$(grep -o 'DANDROID_ABI=[^ ]*' "$ROOT_DIR/$tp" 2>/dev/null | sed 's/DANDROID_ABI=//')
	if [ -z "$ABI" ]; then
		ABI=$(basename "$tp" | sed 's/-toolchain\.params$//')
	fi
	TOOLCHAIN_ABI_LIST="${TOOLCHAIN_ABI_LIST:+$TOOLCHAIN_ABI_LIST }$ABI"
done

# Detect if any toolchain is Android
HAS_ANDROID=0
for tp in $TOOLCHAIN_PARAMS_LIST; do
	if grep -q "DANDROID_ABI=" "$ROOT_DIR/$tp"; then
		HAS_ANDROID=1
		break
	fi
done

# Check Docker availability if Docker build is requested
if [ $USE_DOCKER -eq 1 ]; then
	if ! command -v docker >/dev/null 2>&1; then
		echo "Error: Docker is not installed or not in PATH"
		echo "Install Docker or use --without-docker flag"
		exit 1
	fi

	if ! docker info >/dev/null 2>&1; then
		echo "Error: Docker daemon is not running"
		echo "Start Docker or use --without-docker flag"
		exit 1
	fi

	echo "Docker build: enabled"
else
	echo "Docker build: disabled (local build)"

	# Check for required tools for local build
	for tool in cmake make; do
		if ! command -v $tool >/dev/null 2>&1; then
			echo "Error: Required tool '$tool' not found in PATH"
			exit 1
		fi
	done

	# Ruby 3.0.0+ is needed by the 'make install' step (via ruby-for-android)
	if ! command -v ruby >/dev/null 2>&1; then
		echo "Warning: Ruby not found in PATH (required for ruby-for-android build)"
		echo "Install Ruby 3.0.0+ on the host system"
	fi

	# Check for Android NDK if any toolchain is an Android build
	IS_ANDROID=0
	for tp in $TOOLCHAIN_PARAMS_LIST; do
		if grep -q "DANDROID_ABI=" "$ROOT_DIR/$tp"; then IS_ANDROID=1; break; fi
	done
	if [ $IS_ANDROID -eq 1 ]; then
		if [ -z "$ANDROID_NDK" ]; then
			NDK_BASE="$HOME/Android/Sdk/ndk"
			if [ -d "$NDK_BASE" ]; then
				ANDROID_NDK="$NDK_BASE/$(ls "$NDK_BASE" | tail -n 1)"
			fi
		fi

		if [ ! -d "$ANDROID_NDK" ]; then
			echo "Error: ANDROID_NDK not set or directory not found"
			echo "Set ANDROID_NDK environment variable or install Android NDK"
			exit 1
		fi

		echo "Android NDK: $ANDROID_NDK"
	fi
fi

if [ $TOOLCHAIN_COUNT -eq 1 ]; then
	echo "Toolchain: $TOOLCHAIN_PARAMS_LIST"
else
	echo "Toolchains ($TOOLCHAIN_COUNT):"
	for tp in $TOOLCHAIN_PARAMS_LIST; do
		echo "  - $tp"
	done
	echo "ABIs: $TOOLCHAIN_ABI_LIST"
fi
echo "Prefix: $PREFIX"
echo "Build directory: $BUILD_DIR"
echo "Target directory: $TARGET_DIR"
if echo "$EXTRA_CMAKE_ARGS" | grep -q "BUILD_SHARED_LIBS=ON"; then
	echo "Build mode: Shared libraries (.so)"
elif echo "$EXTRA_CMAKE_ARGS" | grep -q "BUILD_SHARED_LIBS=OFF"; then
	echo "Build mode: Static libraries (.a)"
else
	echo "Build mode: Static libraries (.a) - default"
fi
if echo "$EXTRA_CMAKE_ARGS" | grep -q "CMAKE_BUILD_TYPE=Debug"; then
	echo "Build type: Debug (with debug symbols and logging)"
elif echo "$EXTRA_CMAKE_ARGS" | grep -q "CMAKE_BUILD_TYPE=Release"; then
	echo "Build type: Release (optimized)"
else
	echo "Build type: Default (unspecified)"
fi

# Generate Makefile
echo "Generating Makefile..."

cat > "$ROOT_DIR/Makefile" << 'MAKEFILE_EOF'
# Generated Makefile - DO NOT EDIT
# Run ./configure to regenerate this file

# Use bash instead of sh for bash-specific syntax (e.g., here-strings <<<)
SHELL := /bin/bash

# Configuration
USE_DOCKER=@USE_DOCKER@
TOOLCHAIN_PARAMS_LIST=@TOOLCHAIN_PARAMS_LIST@
TOOLCHAIN_ABI_LIST=@TOOLCHAIN_ABI_LIST@
TOOLCHAIN_COUNT=@TOOLCHAIN_COUNT@
PREFIX=@PREFIX@
BUILD_DIR=@BUILD_DIR@
TARGET_DIR=@TARGET_DIR@
ROOT_DIR=@ROOT_DIR@
EXTRA_CMAKE_ARGS=@EXTRA_CMAKE_ARGS@
HAS_ANDROID=@HAS_ANDROID@
PROJECT_NAME=litergss-everywhere
CONTAINER=$(PROJECT_NAME)_dev
DOCKER_WORKDIR=/opt/current/src
DOCKER_BUILD_DIR=/opt/current/build

.PHONY: all build configure-cmake export install test publish-kmp clean clean-libs clean-downloads clean-artifacts clean-all help sync docker-init docker-start docker-check-running docker-check-volume docker-sync android-example android-example-build android-example-install docker-deps-image download-deps

# Default target
all: build

help:
	@echo "Available targets:"
	@echo "  all (default)      - Build the project"
	@echo "  build              - Build the project (same as 'all')"
	@echo "  sync               - Force sync of source files to Docker (Docker mode only)"
	@echo "  configure-cmake    - Run CMake configuration"
	@echo "  export             - Export build outputs (fat libs + archives) to $(TARGET_DIR)"
	@echo "  install            - Export build outputs and publish KMP module"
	@echo "  test               - Run smoke tests (native builds only, skipped when cross-compiling)"
	@echo "  publish-kmp        - Publish KMP module to Maven Local (called by install)"
	@echo "  android-example-build - Build the Android example app (assembleDebug)"
	@echo "  android-example-install - Build and install the Android example app on device"
	@echo "  android-example    - Alias for android-example-install"
	@echo "  clean              - Clean build artifacts (keeps downloads)"
	@echo "  clean-libs         - Clean library build directories"
	@echo "  clean-downloads    - Remove downloaded source archives"
	@echo "  clean-artifacts    - Remove output directory ($(TARGET_DIR))"
	@echo "  clean-all          - Clean everything (libs + downloads + artifacts)"
	@echo "  clean-rgss_fat_library - Clean the fat library and JNI output directories"
	@echo "  clean-<package>    - Clean specific package (e.g., clean-ruby-for-android)"
	@echo "  logs-<package>-<step> - Show log for package build step (download/configure/build/install)"
	@echo ""
	@echo "Dependency caching:"
	@echo "  docker-deps-image  - Generate and build Docker image with pre-cached dependencies"
	@echo "  download-deps      - Pre-download all dependencies locally (no Docker needed)"

# ============================================================================
# Dependency Caching Targets (work in both Docker and Local modes)
# ============================================================================

# Generate Dockerfile.deps with pre-cached dependencies and build the image
docker-deps-image:
	@echo "Generating Dockerfile with cached dependencies..."
	@cmake \
		-DDEPS_DIR=$(ROOT_DIR)/cmake/litergss-app/dependencies \
		-DBASE_DOCKERFILE=$(ROOT_DIR)/docker/Dockerfile \
		-DOUTPUT_DIR=$(ROOT_DIR)/build/docker \
		-P $(ROOT_DIR)/cmake/docker/GenerateDockerfile.cmake
	@echo ""
	@echo "Building Docker image with cached dependencies..."
	docker build -f $(ROOT_DIR)/build/docker/Dockerfile.deps -t litergss-deps $(ROOT_DIR)/docker/
	@echo ""
	@echo "Done! Image tagged as: litergss-deps"
	@echo "To use it, update docker-compose.yml to reference 'litergss-deps' instead of building from docker/"

# Pre-download all dependencies locally (for non-Docker or offline builds)
download-deps:
	@echo "Pre-downloading all dependencies..."
	@cmake \
		-DDEPS_DIR=$(ROOT_DIR)/cmake/litergss-app/dependencies \
		-DDOWNLOAD_DIR=$(ROOT_DIR)/$(BUILD_DIR)/downloads \
		-DGIT_CACHE_DIR=$(ROOT_DIR)/$(BUILD_DIR)/git-cache \
		-P $(ROOT_DIR)/cmake/docker/PreloadDeps.cmake

ifeq ($(USE_DOCKER),1)

# ============================================================================
# Docker Mode Targets
# ============================================================================

# Check if container is running
docker-check-running:
	@if ! docker ps --filter "name=$(CONTAINER)" --format "{{.Names}}" 2>/dev/null | grep -q "^$(CONTAINER)$$"; then \
		echo "Container $(CONTAINER) is not running"; \
		exit 1; \
	fi

# Check if volume exists
docker-check-volume:
	@if ! docker volume ls --format "{{.Name}}" 2>/dev/null | grep -q "^$(PROJECT_NAME)_currentdata$$"; then \
		echo "Volume $(PROJECT_NAME)_currentdata does not exist, will create"; \
	fi

# Sync sources to Docker volume
docker-sync: docker-check-volume
	@echo "Synchronizing source files to Docker volume..."
	@if docker compose run --rm --remove-orphans init >/dev/null 2>&1; then \
		echo "Source synchronization complete"; \
	else \
		echo "Error: Failed to synchronize sources"; \
		exit 1; \
	fi

# Force sync (removes init containers and performs fresh sync)
sync:
	@echo "Forcing fresh synchronization of source files..."
	@echo "Removing any orphaned init containers..."
	@docker compose rm -f -s -v init 2>/dev/null || true
	@$(MAKE) docker-sync

# Start the development container
docker-start:
	@echo "Starting development container..."
	@if docker compose up -d litergss-dev 2>&1 | grep -q "Started\|Running"; then \
		echo "Development container started"; \
	else \
		if docker ps --filter "name=$(CONTAINER)" --format "{{.Names}}" 2>/dev/null | grep -q "^$(CONTAINER)$$"; then \
			echo "Development container already running"; \
		else \
			echo "Error: Failed to start development container"; \
			exit 1; \
		fi \
	fi
	@echo "Waiting for container to be ready..."
	@max_attempts=30; attempt=0; \
	while [ $$attempt -lt $$max_attempts ]; do \
		if docker exec $(CONTAINER) echo "ready" >/dev/null 2>&1; then \
			echo "Container is ready"; \
			break; \
		fi; \
		attempt=$$((attempt + 1)); \
		sleep 1; \
	done; \
	if [ $$attempt -eq $$max_attempts ]; then \
		echo "Error: Container failed to become ready"; \
		exit 1; \
	fi

# Initialize Docker environment (sync + start)
docker-init:
	@if docker ps --filter "name=$(CONTAINER)" --format "{{.Names}}" 2>/dev/null | grep -q "^$(CONTAINER)$$"; then \
		echo "Container already running, skipping sync"; \
	else \
		$(MAKE) docker-sync; \
		$(MAKE) docker-start; \
	fi

# CMake configuration in Docker
configure-cmake: docker-init
	@echo "Configuring with CMake (Docker mode)..."
	@set -e; \
	toolchains=($(TOOLCHAIN_PARAMS_LIST)); \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!toolchains[@]}; do \
		tp=$${toolchains[$$i]}; \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Configuring ABI: $$abi ($$tp) ==="; \
		echo "Build directory: $$build_dir"; \
		docker exec $(CONTAINER) mkdir -p $$build_dir; \
		docker exec $(CONTAINER) sh -c "cd $$build_dir && \
			export SOURCE_DIR=$(DOCKER_WORKDIR) && \
			TOOLCHAIN_PARAMS_FILE=\$$SOURCE_DIR/$$tp && \
			if [ ! -f \"\$$TOOLCHAIN_PARAMS_FILE\" ]; then \
				echo \"Error: Toolchain parameters file not found: \$$TOOLCHAIN_PARAMS_FILE\"; \
				exit 1; \
			fi && \
			echo \"Using toolchain parameters from: \$$TOOLCHAIN_PARAMS_FILE\" && \
			DEPS_CACHE_ARGS=\"\" && \
			if [ -d /opt/deps-cache/downloads ]; then \
				echo \"Using pre-cached dependencies from /opt/deps-cache/downloads\"; \
				DEPS_CACHE_ARGS=\"-DBUILD_DOWNLOAD_DIR=/opt/deps-cache/downloads\"; \
			fi && \
			rm -f CMakeCache.txt && \
			if grep -q \"DANDROID_ABI=\" \"\$$TOOLCHAIN_PARAMS_FILE\"; then \
				NDK_BASE=/opt/android-ndk && \
				export ANDROID_NDK=\$$NDK_BASE && \
				if [ ! -d \"\$$ANDROID_NDK\" ]; then \
					echo \"Error: ANDROID_NDK not found\"; \
					exit 1; \
				fi && \
				echo \"Android NDK: \$$ANDROID_NDK\" && \
				cmake \$$(cat \$$TOOLCHAIN_PARAMS_FILE) \
					-DCMAKE_TOOLCHAIN_FILE=\$$ANDROID_NDK/build/cmake/android.toolchain.cmake \
					-DTOOLCHAIN_FILE=\$$TOOLCHAIN_PARAMS_FILE \
					\$$DEPS_CACHE_ARGS \
					$(EXTRA_CMAKE_ARGS) \
					\$$SOURCE_DIR; \
			else \
				cmake \$$(cat \$$TOOLCHAIN_PARAMS_FILE) \
					-DTOOLCHAIN_FILE=\$$TOOLCHAIN_PARAMS_FILE \
					\$$DEPS_CACHE_ARGS \
					$(EXTRA_CMAKE_ARGS) \
					\$$SOURCE_DIR; \
			fi"; \
	done

# Build in Docker
build: configure-cmake
	@echo "Building project (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Building ABI: $$abi ==="; \
		docker exec $(CONTAINER) cmake --build $$build_dir; \
	done

test: build
	@echo "Running tests (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Running tests for ABI: $$abi ==="; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target rgss_smoke_test 2>/dev/null && \
			docker exec $(CONTAINER) ctest --test-dir $$build_dir --output-on-failure || \
			echo "  (smoke test not available for this configuration, skipping)"; \
	done

# Export build outputs to a well-defined structure under TARGET_DIR:
#   TARGET_DIR/fat-libs/<abi>/librgss_runtime.a
#   TARGET_DIR/archives/<name>.zip
# This is the contract between build and install — no CMakeCache.txt leaves this step.
export: docker-check-running
	@echo "Exporting build outputs from Docker..."
	@rm -rf $(TARGET_DIR)
	@mkdir -p $(TARGET_DIR)/fat-libs $(TARGET_DIR)/archives
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Exporting build outputs for ABI: $$abi ==="; \
		BUILD_STAGING_DIR=$$(docker exec $(CONTAINER) grep -E "BUILD_STAGING_DIR:" $$build_dir/CMakeCache.txt 2>/dev/null | sed -E 's/.*=(.*)$$/\1/'); \
		if [ -z "$$BUILD_STAGING_DIR" ]; then \
			echo "Error: BUILD_STAGING_DIR not found in CMake cache for $$abi"; \
			exit 1; \
		fi; \
		FAT_LIB="$$BUILD_STAGING_DIR/usr/local/lib/librgss_runtime.a"; \
		if docker exec $(CONTAINER) test -f "$$FAT_LIB"; then \
			mkdir -p $(TARGET_DIR)/fat-libs/$$abi; \
			docker cp $(CONTAINER):"$$FAT_LIB" $(TARGET_DIR)/fat-libs/$$abi/librgss_runtime.a; \
			echo "  fat-libs/$$abi/librgss_runtime.a ($$(du -h $(TARGET_DIR)/fat-libs/$$abi/librgss_runtime.a | cut -f1))"; \
		else \
			echo "  Warning: Fat library not found at $$FAT_LIB"; \
		fi; \
		TARGET_ARCHIVES=$$(docker exec $(CONTAINER) grep -E "TARGET_ARCHIVES:" $$build_dir/CMakeCache.txt 2>/dev/null | sed -E 's/.*=(.*)$$/\1/'); \
		if [ -n "$$TARGET_ARCHIVES" ]; then \
			FAILED=0; \
			IFS=';' read -ra ARCHIVES <<< "$$TARGET_ARCHIVES"; \
			for archive in "$${ARCHIVES[@]}"; do \
				if [ -z "$$archive" ]; then continue; fi; \
				ARCHIVE_PATH="$$BUILD_STAGING_DIR/$$archive"; \
				if docker exec $(CONTAINER) test -f "$$ARCHIVE_PATH"; then \
					docker cp $(CONTAINER):"$$ARCHIVE_PATH" $(TARGET_DIR)/archives/ 2>/dev/null || { echo "  Warning: Failed to copy $$archive"; FAILED=1; }; \
					echo "  archives/$$archive"; \
				else \
					echo "  ERROR: Archive not found: $$ARCHIVE_PATH"; \
					FAILED=1; \
				fi; \
			done; \
			if [ $$FAILED -eq 1 ]; then \
				echo "Error: One or more archives are missing or failed to copy"; \
				exit 1; \
			fi; \
		fi; \
	done
	@echo ""
	@echo "Build outputs exported to: ./$(TARGET_DIR)"
	@find $(TARGET_DIR) -type f | sort | awk '{print "  " $$0}'

# Install = export + publish
install: export
	@$(MAKE) publish-kmp

# Publish KMP module to Maven Local (Docker mode)
# Reads fat libs from TARGET_DIR/fat-libs/<abi>/ (produced by 'make export'),
# copies them into the Docker container, and runs the publish script inside it.
publish-kmp: docker-check-running
	@echo ""
	@echo "=========================================="
	@echo "Publishing KMP module (Docker)..."
	@echo "=========================================="
	@if [ ! -d "$(TARGET_DIR)/fat-libs" ]; then \
		echo "Error: $(TARGET_DIR)/fat-libs not found. Run 'make export' first."; \
		exit 1; \
	fi
	@IS_ANDROID=false; IS_DESKTOP=false; \
	for tp in $(TOOLCHAIN_PARAMS_LIST); do \
		if grep -q "DANDROID_ABI=" "$$tp" 2>/dev/null; then IS_ANDROID=true; \
		elif grep -q "DCMAKE_SYSTEM_NAME=Linux" "$$tp" 2>/dev/null; then IS_DESKTOP=true; \
		fi; \
	done; \
	set -e; \
	DOCKER_TARGET_DIR="$(DOCKER_WORKDIR)/$(TARGET_DIR)"; \
	docker exec $(CONTAINER) mkdir -p "$$DOCKER_TARGET_DIR/fat-libs"; \
	DOCKER_SCRIPT_ARGS="--root-dir $(DOCKER_WORKDIR) --target-dir $$DOCKER_TARGET_DIR"; \
	for abi_dir in $(TARGET_DIR)/fat-libs/*/; do \
		[ -d "$$abi_dir" ] || continue; \
		abi=$$(basename "$$abi_dir"); \
		fat_lib="$$abi_dir/librgss_runtime.a"; \
		if [ -f "$$fat_lib" ]; then \
			docker exec $(CONTAINER) mkdir -p "$$DOCKER_TARGET_DIR/fat-libs/$$abi"; \
			docker cp "$$fat_lib" $(CONTAINER):"$$DOCKER_TARGET_DIR/fat-libs/$$abi/librgss_runtime.a"; \
			DOCKER_SCRIPT_ARGS="$$DOCKER_SCRIPT_ARGS --abi $$abi --fat-lib $$DOCKER_TARGET_DIR/fat-libs/$$abi/librgss_runtime.a"; \
		fi; \
	done; \
	docker cp $(ROOT_DIR)/scripts/. $(CONTAINER):$(DOCKER_WORKDIR)/scripts/; \
	docker cp $(ROOT_DIR)/external/embedded-ruby-vm/kmp-publish/. $(CONTAINER):$(DOCKER_WORKDIR)/external/embedded-ruby-vm/kmp-publish/; \
	docker cp $(ROOT_DIR)/external/embedded-ruby-vm/kmp/src $(CONTAINER):$(DOCKER_WORKDIR)/external/embedded-ruby-vm/kmp/; \
	if [ "$$IS_ANDROID" = "true" ]; then \
		docker exec $(CONTAINER) bash -c "$(DOCKER_WORKDIR)/scripts/publish-kmp-android.sh $$DOCKER_SCRIPT_ARGS --ndk-home /opt/android-ndk"; \
	elif [ "$$IS_DESKTOP" = "true" ]; then \
		docker exec $(CONTAINER) bash -c "$(DOCKER_WORKDIR)/scripts/publish-kmp-desktop.sh $$DOCKER_SCRIPT_ARGS"; \
	else \
		echo "No KMP publishing needed for this platform"; \
	fi
	@echo ""
	@echo "Copying Maven artifacts from Docker..."
	@mkdir -p $(TARGET_DIR)/maven
	@docker exec $(CONTAINER) find /root/.m2/repository/com/scorbutics/rubyvm -name '*.aar' -o -name '*.jar' -o -name '*.pom' 2>/dev/null | head -5 || true
	@if docker exec $(CONTAINER) test -d /root/.m2/repository/com/scorbutics/rubyvm; then \
		mkdir -p $(TARGET_DIR)/maven/com/scorbutics/rubyvm/; \
		docker cp $(CONTAINER):/root/.m2/repository/com/scorbutics/rubyvm/. $(TARGET_DIR)/maven/com/scorbutics/rubyvm/; \
		echo "Maven artifacts exported to: ./$(TARGET_DIR)/maven/"; \
		mkdir -p $(HOME)/.m2/repository/com/scorbutics/rubyvm/; \
		cp -r $(TARGET_DIR)/maven/com/scorbutics/rubyvm/. $(HOME)/.m2/repository/com/scorbutics/rubyvm/; \
		echo "Maven artifacts installed to: ~/.m2/repository/"; \
	else \
		echo "Warning: No Maven artifacts found in container"; \
	fi

# Build Android example app
android-example-build: docker-check-running
	@echo ""
	@echo "=========================================="
	@echo "Building Android example app..."
	@echo "=========================================="
	@# Sync the example project to the container
	@docker exec $(CONTAINER) mkdir -p $(DOCKER_WORKDIR)/examples/android-integration
	@docker cp $(ROOT_DIR)/examples/android-integration/. $(CONTAINER):$(DOCKER_WORKDIR)/examples/android-integration/
	@# Set up local.properties for Android SDK
	@echo "sdk.dir=/opt/android-sdk" > $(ROOT_DIR)/examples/android-integration/local.properties
	@docker cp $(ROOT_DIR)/examples/android-integration/local.properties $(CONTAINER):$(DOCKER_WORKDIR)/examples/android-integration/local.properties
	@# Build the app
	@if docker exec $(CONTAINER) bash -c "cd $(DOCKER_WORKDIR)/examples/android-integration && ./gradlew assembleDebug" 2>&1; then \
		echo ""; \
		echo "✓ Android example app built successfully!"; \
		echo "  APK location (in container): $(DOCKER_WORKDIR)/examples/android-integration/app/build/outputs/apk/debug/app-debug.apk"; \
		docker exec $(CONTAINER) ls -lh $(DOCKER_WORKDIR)/examples/android-integration/app/build/outputs/apk/debug/app-debug.apk 2>/dev/null || true; \
		echo ""; \
		echo "To copy APK to host:"; \
		echo "  docker cp $(CONTAINER):$(DOCKER_WORKDIR)/examples/android-integration/app/build/outputs/apk/debug/app-debug.apk ./"; \
		echo ""; \
	else \
		echo ""; \
		echo "✗ Android example build failed - see error output above"; \
		exit 1; \
	fi

# Build and install Android example app on device
android-example-install: docker-check-running
	@echo ""
	@echo "=========================================="
	@echo "Building and installing Android example..."
	@echo "=========================================="
	@# Sync the example project to the container
	@docker exec $(CONTAINER) mkdir -p $(DOCKER_WORKDIR)/examples/android-integration
	@docker cp $(ROOT_DIR)/examples/android-integration/. $(CONTAINER):$(DOCKER_WORKDIR)/examples/android-integration/
	@# Set up local.properties for Android SDK
	@echo "sdk.dir=/opt/android-sdk" > $(ROOT_DIR)/examples/android-integration/local.properties
	@docker cp $(ROOT_DIR)/examples/android-integration/local.properties $(CONTAINER):$(DOCKER_WORKDIR)/examples/android-integration/local.properties
	@# Build and install the app
	@echo "NOTE: This requires an Android device or emulator connected via ADB"
	@echo "      Make sure ADB is accessible from the container"
	@echo ""
	@if docker exec $(CONTAINER) bash -c "cd $(DOCKER_WORKDIR)/examples/android-integration && ./gradlew installDebug" 2>&1; then \
		echo ""; \
		echo "✓ Android example app installed successfully!"; \
		echo "  App package: com.example.rgss"; \
		echo ""; \
		echo "To launch the app from command line:"; \
		echo "  adb shell am start -n com.example.rgss/.MainActivity"; \
		echo ""; \
	else \
		echo ""; \
		echo "✗ Android example installation failed"; \
		echo "  Make sure an Android device/emulator is connected"; \
		echo "  Check ADB connectivity: adb devices"; \
		exit 1; \
	fi

# Alias for android-example-install
android-example: android-example-install

# Clean targets in Docker
clean: docker-init
	@echo "Cleaning build artifacts (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target clean 2>/dev/null || true; \
	done

clean-libs: docker-init
	@echo "Cleaning library builds (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target clean-libs 2>/dev/null || true; \
	done

clean-downloads: docker-init
	@echo "Cleaning downloads (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target clean-downloads 2>/dev/null || true; \
	done

clean-artifacts: docker-init
	@echo "Cleaning artifacts (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target clean-artifacts 2>/dev/null || true; \
	done
	@rm -rf $(TARGET_DIR)

clean-all: clean-libs clean-downloads clean-artifacts
	@echo "Full clean complete (Docker mode)"

# Clean fat library target (Docker mode)
clean-rgss_fat_library: docker-init
	@echo "Cleaning fat library (Docker mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(DOCKER_BUILD_DIR); \
		else \
			build_dir=$(DOCKER_BUILD_DIR)-$$abi; \
		fi; \
		docker exec $(CONTAINER) cmake --build $$build_dir --target rgss_fat_library_clean 2>/dev/null || true; \
	done

# Package-specific clean targets (Docker mode)
@PACKAGE_CLEAN_TARGETS_DOCKER@

# Package-specific log extraction targets (Docker mode)
@PACKAGE_LOG_TARGETS_DOCKER@

else

# ============================================================================
# Local Mode Targets (without Docker)
# ============================================================================

configure-cmake:
	@echo "Configuring with CMake (local mode)..."
	@set -e; \
	toolchains=($(TOOLCHAIN_PARAMS_LIST)); \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!toolchains[@]}; do \
		tp=$${toolchains[$$i]}; \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Configuring ABI: $$abi ($$tp) ==="; \
		echo "Build directory: $$build_dir"; \
		mkdir -p $$build_dir; \
		cd $$build_dir && \
			TOOLCHAIN_PARAMS_FILE="../$$tp" && \
			if [ ! -f "$$TOOLCHAIN_PARAMS_FILE" ]; then \
				echo "Error: Toolchain parameters file not found: $$TOOLCHAIN_PARAMS_FILE"; \
				exit 1; \
			fi && \
			echo "Using toolchain parameters from: $$TOOLCHAIN_PARAMS_FILE" && \
			DEPS_CACHE_ARGS="" && \
			if [ -d "$(ROOT_DIR)/$(BUILD_DIR)/downloads" ]; then \
				echo "Using pre-cached dependencies from $(ROOT_DIR)/$(BUILD_DIR)/downloads"; \
				DEPS_CACHE_ARGS="-DBUILD_DOWNLOAD_DIR=$(ROOT_DIR)/$(BUILD_DIR)/downloads"; \
			fi && \
			rm -f CMakeCache.txt && \
			if grep -q "DANDROID_ABI=" "$$TOOLCHAIN_PARAMS_FILE"; then \
				NDK_BASE=$$HOME/Android/Sdk/ndk && \
				export ANDROID_NDK=$${ANDROID_NDK:-$$NDK_BASE/$$(ls $$NDK_BASE | tail -n 1)} && \
				if [ ! -d "$$ANDROID_NDK" ]; then \
					echo "Error: ANDROID_NDK not found"; \
					exit 1; \
				fi && \
				echo "Android NDK: $$ANDROID_NDK" && \
				cmake $$(cat $$TOOLCHAIN_PARAMS_FILE) \
					-DCMAKE_TOOLCHAIN_FILE=$$ANDROID_NDK/build/cmake/android.toolchain.cmake \
					-DTOOLCHAIN_FILE=$$TOOLCHAIN_PARAMS_FILE \
					$$DEPS_CACHE_ARGS \
					$(EXTRA_CMAKE_ARGS) \
					..; \
			else \
				cmake $$(cat $$TOOLCHAIN_PARAMS_FILE) \
					-DTOOLCHAIN_FILE=$$TOOLCHAIN_PARAMS_FILE \
					$$DEPS_CACHE_ARGS \
					$(EXTRA_CMAKE_ARGS) \
					..; \
			fi; \
		cd ..; \
	done

build: configure-cmake
	@echo "Building project (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Building ABI: $$abi ==="; \
		cmake --build $$build_dir; \
	done

test: build
	@echo "Running tests (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		echo ""; \
		echo "=== Running tests for ABI: $$abi ==="; \
		cmake --build $$build_dir --target rgss_smoke_test 2>/dev/null && \
			ctest --test-dir $$build_dir --output-on-failure || \
			echo "  (smoke test not available for this configuration, skipping)"; \
	done

# Export build outputs to a well-defined structure under TARGET_DIR:
#   TARGET_DIR/fat-libs/<abi>/librgss_runtime.a
#   TARGET_DIR/archives/<name>.zip
# This is the contract between build and install — no CMakeCache.txt leaves this step.
export:
	@echo "Exporting build outputs (local mode)..."
	@rm -rf $(TARGET_DIR)
	@mkdir -p $(TARGET_DIR)/fat-libs $(TARGET_DIR)/archives
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ ! -d "$$build_dir" ]; then \
			echo "Error: Build directory not found: $$build_dir. Run 'make build' first."; \
			exit 1; \
		fi; \
		echo ""; \
		echo "=== Exporting build outputs for ABI: $$abi ==="; \
		BUILD_STAGING_DIR=$$(grep -E "BUILD_STAGING_DIR:" $$build_dir/CMakeCache.txt 2>/dev/null | sed -E 's/.*=(.*)$$/\1/'); \
		if [ -z "$$BUILD_STAGING_DIR" ]; then \
			echo "Error: BUILD_STAGING_DIR not found in CMake cache for $$abi"; \
			exit 1; \
		fi; \
		FAT_LIB="$$BUILD_STAGING_DIR/usr/local/lib/librgss_runtime.a"; \
		if [ -f "$$FAT_LIB" ]; then \
			mkdir -p $(TARGET_DIR)/fat-libs/$$abi; \
			cp "$$FAT_LIB" $(TARGET_DIR)/fat-libs/$$abi/librgss_runtime.a; \
			echo "  fat-libs/$$abi/librgss_runtime.a ($$(du -h "$$FAT_LIB" | cut -f1))"; \
		else \
			echo "  Warning: Fat library not found at $$FAT_LIB"; \
		fi; \
		TARGET_ARCHIVES=$$(grep -E "TARGET_ARCHIVES:" $$build_dir/CMakeCache.txt 2>/dev/null | sed -E 's/.*=(.*)$$/\1/'); \
		if [ -n "$$TARGET_ARCHIVES" ]; then \
			FAILED=0; \
			IFS=';' read -ra ARCHIVES <<< "$$TARGET_ARCHIVES"; \
			for archive in "$${ARCHIVES[@]}"; do \
				if [ -z "$$archive" ]; then continue; fi; \
				ARCHIVE_PATH="$$BUILD_STAGING_DIR/$$archive"; \
				if [ -f "$$ARCHIVE_PATH" ]; then \
					cp "$$ARCHIVE_PATH" $(TARGET_DIR)/archives/ 2>/dev/null || { echo "  Warning: Failed to copy $$archive"; FAILED=1; }; \
					echo "  archives/$$archive"; \
				else \
					echo "  ERROR: Archive not found: $$ARCHIVE_PATH"; \
					FAILED=1; \
				fi; \
			done; \
			if [ $$FAILED -eq 1 ]; then \
				echo "Error: One or more archives are missing or failed to copy"; \
				exit 1; \
			fi; \
		fi; \
	done
	@echo ""
	@echo "Build outputs exported to: ./$(TARGET_DIR)"
	@find $(TARGET_DIR) -type f | sort | awk '{print "  " $$0}'

# Install = export + publish
install: export
	@$(MAKE) publish-kmp

# Publish KMP module to Maven Local (local mode)
# Reads fat libs from TARGET_DIR/fat-libs/<abi>/ (produced by 'make export')
# and delegates to the appropriate publish script based on platform.
publish-kmp:
	@echo ""
	@echo "=========================================="
	@echo "Publishing KMP module..."
	@echo "=========================================="
	@if [ ! -d "$(TARGET_DIR)/fat-libs" ]; then \
		echo "Error: $(TARGET_DIR)/fat-libs not found. Run 'make export' first."; \
		exit 1; \
	fi
	@IS_ANDROID=false; IS_IOS=false; IS_DESKTOP=false; \
	for tp in $(TOOLCHAIN_PARAMS_LIST); do \
		if grep -q "DANDROID_ABI=" "$$tp" 2>/dev/null; then IS_ANDROID=true; \
		elif grep -q "DCMAKE_SYSTEM_NAME=iOS" "$$tp" 2>/dev/null; then IS_IOS=true; \
		elif grep -q "DCMAKE_SYSTEM_NAME=Linux" "$$tp" 2>/dev/null; then IS_DESKTOP=true; \
		fi; \
	done; \
	SCRIPT_ARGS="--root-dir $(ROOT_DIR) --target-dir $(TARGET_DIR)"; \
	for abi_dir in $(TARGET_DIR)/fat-libs/*/; do \
		[ -d "$$abi_dir" ] || continue; \
		abi=$$(basename "$$abi_dir"); \
		fat_lib="$$abi_dir/librgss_runtime.a"; \
		if [ -f "$$fat_lib" ]; then \
			SCRIPT_ARGS="$$SCRIPT_ARGS --abi $$abi --fat-lib $$fat_lib"; \
		fi; \
	done; \
	if [ "$$IS_ANDROID" = "true" ]; then \
		set -e; \
		$(ROOT_DIR)/scripts/publish-kmp-android.sh $$SCRIPT_ARGS; \
	elif [ "$$IS_DESKTOP" = "true" ]; then \
		set -e; \
		$(ROOT_DIR)/scripts/publish-kmp-desktop.sh $$SCRIPT_ARGS; \
	elif [ "$$IS_IOS" = "true" ]; then \
		set -e; \
		$(ROOT_DIR)/scripts/publish-kmp-ios.sh $$SCRIPT_ARGS; \
	else \
		echo "No KMP publishing needed for this platform"; \
	fi

clean:
	@echo "Cleaning build artifacts (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ -d "$$build_dir" ]; then \
			cmake --build $$build_dir --target clean 2>/dev/null || true; \
		fi; \
	done

clean-libs:
	@echo "Cleaning library builds (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ -d "$$build_dir" ]; then \
			cmake --build $$build_dir --target clean-libs 2>/dev/null || true; \
		fi; \
	done

clean-downloads:
	@echo "Cleaning downloads (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ -d "$$build_dir" ]; then \
			cmake --build $$build_dir --target clean-downloads 2>/dev/null || true; \
		fi; \
	done

clean-artifacts:
	@echo "Cleaning artifacts (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ -d "$$build_dir" ]; then \
			cmake --build $$build_dir --target clean-artifacts 2>/dev/null || true; \
		fi; \
	done
	@rm -rf $(TARGET_DIR)

clean-all: clean-libs clean-downloads clean-artifacts
	@echo "Full clean complete (local mode)"
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		rm -rf $$build_dir; \
	done

# Clean fat library target (local mode)
clean-rgss_fat_library:
	@echo "Cleaning fat library (local mode)..."
	@set -e; \
	abis=($(TOOLCHAIN_ABI_LIST)); \
	for i in $${!abis[@]}; do \
		abi=$${abis[$$i]}; \
		if [ $(TOOLCHAIN_COUNT) -eq 1 ]; then \
			build_dir=$(BUILD_DIR); \
		else \
			build_dir=$(BUILD_DIR)-$$abi; \
		fi; \
		if [ -d "$$build_dir" ]; then \
			cmake --build $$build_dir --target rgss_fat_library_clean 2>/dev/null || true; \
		fi; \
	done

# Package-specific clean targets (Local mode)
@PACKAGE_CLEAN_TARGETS_LOCAL@

# Package-specific log extraction targets (Local mode)
@PACKAGE_LOG_TARGETS_LOCAL@

sync:
	@echo "Warning: 'sync' target only works in Docker mode"
	@echo "Source files are already in the current directory (local mode)"

endif

MAKEFILE_EOF

# Extract package list from CMakeLists.txt to generate clean targets
PACKAGES=""
if [ -f "$ROOT_DIR/CMakeLists.txt" ]; then
	PACKAGES=$(grep -E 'APP_DEPENDENCIES\s+"' "$ROOT_DIR/CMakeLists.txt" | sed -E 's/.*APP_DEPENDENCIES\s+"([^"]+)".*/\1/' | tr ';' ' ')
fi

# Generate package-specific clean targets for Docker mode
PACKAGE_CLEAN_DOCKER=""
if [ -n "$PACKAGES" ]; then
	for pkg in $PACKAGES; do
		PACKAGE_CLEAN_DOCKER="${PACKAGE_CLEAN_DOCKER}
clean-${pkg}: docker-init
	@echo \"Cleaning ${pkg} (Docker mode)...\"
	@set -e; \\
	abis=(\$(TOOLCHAIN_ABI_LIST)); \\
	for i in \$\${!abis[@]}; do \\
		abi=\$\${abis[\$$i]}; \\
		if [ \$(TOOLCHAIN_COUNT) -eq 1 ]; then \\
			build_dir=\$(DOCKER_BUILD_DIR); \\
		else \\
			build_dir=\$(DOCKER_BUILD_DIR)-\$$abi; \\
		fi; \\
		docker exec \$(CONTAINER) cmake --build \$$build_dir --target ${pkg}_clean 2>/dev/null || true; \\
	done

"
	done
fi

# Generate package-specific clean targets for Local mode
PACKAGE_CLEAN_LOCAL=""
if [ -n "$PACKAGES" ]; then
	for pkg in $PACKAGES; do
		PACKAGE_CLEAN_LOCAL="${PACKAGE_CLEAN_LOCAL}
clean-${pkg}:
	@echo \"Cleaning ${pkg} (local mode)...\"
	@set -e; \\
	abis=(\$(TOOLCHAIN_ABI_LIST)); \\
	for i in \$\${!abis[@]}; do \\
		abi=\$\${abis[\$$i]}; \\
		if [ \$(TOOLCHAIN_COUNT) -eq 1 ]; then \\
			build_dir=\$(BUILD_DIR); \\
		else \\
			build_dir=\$(BUILD_DIR)-\$$abi; \\
		fi; \\
		if [ -d \"\$$build_dir\" ]; then \\
			cmake --build \$$build_dir --target ${pkg}_clean 2>/dev/null || true; \\
		fi; \\
	done

"
	done
fi

# Generate package-specific log extraction targets for Docker mode
PACKAGE_LOG_DOCKER=""
if [ -n "$PACKAGES" ]; then
	for pkg in $PACKAGES; do
		for step in download configure build install; do
			PACKAGE_LOG_DOCKER="${PACKAGE_LOG_DOCKER}
logs-${pkg}-${step}: docker-check-running
	@echo \"=== ${pkg} ${step} logs ===\"
	@set -e; \\
	abis=(\$(TOOLCHAIN_ABI_LIST)); \\
	for i in \$\${!abis[@]}; do \\
		abi=\$\${abis[\$$i]}; \\
		if [ \$(TOOLCHAIN_COUNT) -eq 1 ]; then \\
			build_dir=\$(DOCKER_BUILD_DIR); \\
		else \\
			build_dir=\$(DOCKER_BUILD_DIR)-\$$abi; \\
			echo \"--- ABI: \$$abi ---\"; \\
		fi; \\
		docker exec \$(CONTAINER) sh -c \"cat \$$build_dir/${pkg}/build_dir/*/stamps/${pkg}_external-${step}-*.log 2>/dev/null\" || echo \"No ${step} logs found for ${pkg} (\$$abi)\"; \\
	done

"
		done
	done
fi

# Generate package-specific log extraction targets for Local mode
PACKAGE_LOG_LOCAL=""
if [ -n "$PACKAGES" ]; then
	for pkg in $PACKAGES; do
		for step in download configure build install; do
			PACKAGE_LOG_LOCAL="${PACKAGE_LOG_LOCAL}
logs-${pkg}-${step}:
	@echo \"=== ${pkg} ${step} logs ===\"
	@set -e; \\
	abis=(\$(TOOLCHAIN_ABI_LIST)); \\
	for i in \$\${!abis[@]}; do \\
		abi=\$\${abis[\$$i]}; \\
		if [ \$(TOOLCHAIN_COUNT) -eq 1 ]; then \\
			build_dir=\$(BUILD_DIR); \\
		else \\
			build_dir=\$(BUILD_DIR)-\$$abi; \\
			echo \"--- ABI: \$$abi ---\"; \\
		fi; \\
		cat \$$build_dir/${pkg}/build_dir/*/stamps/${pkg}_external-${step}-*.log 2>/dev/null || echo \"No ${step} logs found for ${pkg} (\$$abi)\"; \\
	done

"
		done
	done
fi

# Create temporary files with the package clean targets
cat > "$ROOT_DIR/Makefile.tmp" << EOF
$PACKAGE_CLEAN_DOCKER
EOF

cat > "$ROOT_DIR/Makefile.local.tmp" << EOF
$PACKAGE_CLEAN_LOCAL
EOF

# Create temporary files with the package log targets
cat > "$ROOT_DIR/Makefile.log.tmp" << EOF
$PACKAGE_LOG_DOCKER
EOF

cat > "$ROOT_DIR/Makefile.log.local.tmp" << EOF
$PACKAGE_LOG_LOCAL
EOF

# Substitute configuration values
sed \
	-e "s|@USE_DOCKER@|$USE_DOCKER|g" \
	-e "s|@TOOLCHAIN_PARAMS_LIST@|$TOOLCHAIN_PARAMS_LIST|g" \
	-e "s|@TOOLCHAIN_ABI_LIST@|$TOOLCHAIN_ABI_LIST|g" \
	-e "s|@TOOLCHAIN_COUNT@|$TOOLCHAIN_COUNT|g" \
	-e "s|@PREFIX@|$PREFIX|g" \
	-e "s|@BUILD_DIR@|$BUILD_DIR|g" \
	-e "s|@TARGET_DIR@|$TARGET_DIR|g" \
	-e "s|@ROOT_DIR@|$ROOT_DIR|g" \
	-e "s|@EXTRA_CMAKE_ARGS@|$EXTRA_CMAKE_ARGS|g" \
	-e "s|@HAS_ANDROID@|$HAS_ANDROID|g" \
	"$ROOT_DIR/Makefile" > "$ROOT_DIR/Makefile.new"

# Insert package clean and log targets
awk '
	/@PACKAGE_CLEAN_TARGETS_DOCKER@/ {
		while ((getline line < "'"$ROOT_DIR"'/Makefile.tmp") > 0)
			print line
		next
	}
	/@PACKAGE_CLEAN_TARGETS_LOCAL@/ {
		while ((getline line < "'"$ROOT_DIR"'/Makefile.local.tmp") > 0)
			print line
		next
	}
	/@PACKAGE_LOG_TARGETS_DOCKER@/ {
		while ((getline line < "'"$ROOT_DIR"'/Makefile.log.tmp") > 0)
			print line
		next
	}
	/@PACKAGE_LOG_TARGETS_LOCAL@/ {
		while ((getline line < "'"$ROOT_DIR"'/Makefile.log.local.tmp") > 0)
			print line
		next
	}
	{ print }
' "$ROOT_DIR/Makefile.new" > "$ROOT_DIR/Makefile.final"

mv "$ROOT_DIR/Makefile.final" "$ROOT_DIR/Makefile"
rm -f "$ROOT_DIR/Makefile.new" "$ROOT_DIR/Makefile.tmp" "$ROOT_DIR/Makefile.local.tmp" "$ROOT_DIR/Makefile.log.tmp" "$ROOT_DIR/Makefile.log.local.tmp"

echo ""
echo "Configuration complete!"
echo ""
echo "Next steps:"
echo "  make           # Build the project"
echo "  make install   # Export/install build artifacts"
echo ""
echo "Run 'make help' to see all available targets."
